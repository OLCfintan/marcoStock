import 'dart:io';
import 'package:sqlite3/sqlite3.dart';

void main() {
  final db = sqlite3.open('marcoStock_db.sqlite'); // I don't know the exact path.
  // Actually, I can't easily connect to their db.
}
